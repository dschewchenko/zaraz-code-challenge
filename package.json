{
  "name": "tic-tac-toe",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "mkdir -p ./kv &&cd components/tic-tac-toe && npm install && npm run bundle && cd ../.. && npx webcm",
    "prepare": "husky",
    "husky:pre-commit": "exit 0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.5"
  }
}
