{
  "name": "Tic Tac Toe Game",
  "namespace": "tic-tac-toe",
  "description": "Returns an tic-tac-toe game widget",
  "icon": "assets/icon.svg",
  "allowCustomFields": true,
  "permissions": {
    "execute_unsafe_scripts": {
      "description": "For debug purposes only",
      "required": false
    },
    "access_client_kv": {
      "description": "We need this permission to store the game state",
      "required": true
    },
    "server_network_requests": {
      "description": "This permission is used to facilitate better a user experience.",
      "required": true
    },
    "serve_static_files": {
      "description": "We need this permission to serve the widget files",
      "required": true
    },
    "provide_server_functionality": {
      "description": "We need this permission to make requests to the server. Player's opponent will be the computer",
      "required": true
    },
    "provide_widget": {
      "description": "We need this permission to render the widget with game",
      "required": true
    }
  }
}
